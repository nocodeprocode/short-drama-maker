# Live provider artifacts

This folder is written by `npm run validate:providers`. It is gitignored except this file.

Files here are real OpenRouter / ElevenLabs / Stripe outputs, not fixtures. Open `index.html` after a validation run to inspect them.
